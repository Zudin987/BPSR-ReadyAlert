namespace BPSR.ReadyAlert;

internal sealed partial class ChatGeneralSettingsForm
{
    // Logical 96-DPI design targets. WinForms owns monitor scaling at runtime;
    // regression tests validate these compact design limits separately from the
    // post-layout usability checks that run against the actual scaled controls.
    internal const int V122LogicalSidebarWidth = 176;
    internal const int V122LogicalFooterHeight = 60;
    internal const int V122LogicalNavHeight = 36;
    internal const int V122LogicalMaxRuleHeight = 70;
    internal const int V122LogicalMaxInputWidth = 340;
}
