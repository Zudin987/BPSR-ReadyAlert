namespace BPSR.ReadyAlert;

internal sealed partial class ChatGeneralSettingsForm
{
    // Logical 96-DPI density targets used by the compact Settings implementation.
    // WinForms owns monitor scaling at runtime; post-layout smoke tests verify that
    // actual scaled controls stay inside the minimum-size client area.
    internal const int V122LogicalSidebarWidth = 0;
    internal const int V122LogicalFooterHeight = 52;
    internal const int V122LogicalNavHeight = 28;
    internal const int V122LogicalMaxRuleHeight = 54;
    internal const int V122LogicalMaxInputWidth = 300;
}
